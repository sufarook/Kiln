# Gradle
.gradle/
build/
.kotlin/
*.hprof
captures/

# IDE
.idea/
*.iml

# Android local config (contains SDK path — machine-specific)
local.properties

# Signing & secrets — never commit these
*.keystore
*.jks
*.p12
*.gpg
*.asc
gradle.properties.local
keystore.properties

# macOS
.DS_Store
**/.DS_Store

# Xcode / KMP iOS builds
xcuserdata/
*.xcworkspace/xcuserdata/
DerivedData/
.swiftpm/

# Claude Code local settings (per-developer permissions, not shared)
.claude/settings.local.json
